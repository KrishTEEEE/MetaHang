# Dependencies
node_modules/

# Build output
dist/

# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# Editor / IDE
.vscode/
.idea/
*.swp

# Logs
*.log
npm-debug.log*

# Secrets and local environment — never commit these
.env
.env.*
!.env.example
*.pem
*.key

# Claude Code local state: contains absolute paths and session ids.
# launch.json is kept deliberately — it is the shared dev-server config.
.claude/*
!.claude/launch.json
